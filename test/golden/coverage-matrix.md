# Golden-master coverage matrix (Phase 0b)

Every required surface has a row. **FINAL STATE (DELTA-STRENGTH close, 2026-06-05):
all rows are TICKED** at `PINNED_TS_COMMIT = 8c59ec4` — the TS-replayable gate set
is **19 rows** (23 MATCH-PROOF files counting the platform-split .macos/.linux
siblings), plus 1 EXPECTATION-ONLY Rust-target row outside the TS-replay set.
Zero `EXCLUDED` rows, zero vacuous ticks. (The Part-1 "all UNticked / blocked until
pin" prose formerly here is historical — see git history.)

Legend: `[x]` = double-recorded at pin + admitted + replays green via verify.sh
in a fresh jail (macOS Darwin arm64, Step 2). `[ ]` = not yet recorded.
`EXCLUDED-<reason>` = honest non-recordable (rider R4), never a vacuous tick.
`EXPECTATION-ONLY` = a RUST-TARGET row with NO symmetric TS counterpart at the pin
(sanctioned by ADR-0011 §(a)); it pins what the Rust engine MUST produce, is NOT
recorded against TS (that would launder the bug it catches into gold), and stays
UNticked until the Rust engine exists to DRIVE it. NOT wired into the green gate.
Stub-backed rows (§S) carry stub_sha256 in RECORDED-FROM + MATCH-PROOF (R1).
`class` per ADR 0004.
`PROVISIONAL` = touches the in-flight fix-wave (send:pty queue-to-busy / `--wait` /
relay, ADD-2) — re-verify at pin.

**0b DELTA-STRENGTH W3a (2026-06-05):** four rows STRENGTHENED + re-recorded at pin
8c59ec4 against stub 1.8.0 (sha 581ad3c): relay-health (W3.2 P1, value-bearing
pre-normalization cross-checks), new-session-trace (W3.3 P2, went-busy + pre-PID
count + decoy), history + attach-detach-reattach (W3.8 P12, backlog content-integrity:
sentinel byte-exact + exactly-once multiset). Every new assertion has a biting mutant
in `run_mutation_real.sh` (zero false negatives). **send-pty-paste-burst (W3.1 P3) is
RESOLVED** (orc-4 Option C ruling 14:50 EDT): the busy row is STRENGTHENED at sub-1KB
(ordered user-record TEXTS + anchor + reply byte-exact), and the ≥4KB chunked path is
covered by a NEW IDLE-path row (`send-pty-chunked-idle`) under the narrow env-gated
`STUB_RAW_STDIN` recording mode (stub 1.8.1). The premise correction (cooked-mode
macOS MAX_CANON=1024 drops a ≥4KB single-canonical-line write; real Claude runs raw)
is named precisely in the ADR-0011 W3.1-RULED addendum.
**CHUNKED-PATH COVERAGE (orc-required statement) — HOLE NOW CLOSED IN-CORPUS:** the
≥4KB chunked PTY path IS covered by `send-pty-chunked-idle` (the full payload lands
byte-exact across 5 chunks; truncated-burst + cooked-drop C2 mutants bite). The A4
fake-REPL `DROP_OVER_BYTES` gate (ADR-0009) now carries only the REALISM dimension
(real Claude / real tty queue / real timing) per ADR-0011 §(a)'s parity-vs-realism
split, plus the busy-path ≥4KB realism the cooked oracle cannot host.

**0b DELTA-STRENGTH W4 (2026-06-05):** the stale-pin re-record + dual-platform close.
**W4.1 (macOS, pin re-mint):** build-claude-cmd, exit-codes, ls-info-json,
zmx-dir-resolution (macOS sibling) RE-RECORDED at 8c59ec4 — the red-team M2 resolution
(MATCH-PROOF's `pinned_ts_commit` is recording-time truth; only a TRUE re-record
re-mints it, a re-stamp may only touch stub-sha lines). All normalized content
BYTE-IDENTICAL to the committed golden (engine surfaces inert across 0d0fa9e..8c59ec4);
provenance re-minted, RECORDED-FROM's `verified_diff_carry_from`/`restamped` lines
DROPPED. attach-detach-reattach + history were already re-minted by W3.8. **W4.2
(Linux, in-VM):** zmx-dir-resolution-linux re-record (re-mints the .linux MATCH-PROOF
0d0fa9e→8c59ec4) + termios-report-linux NEW recording (the .linux sibling of W3.7);
copy-back through platform-aware fixture_admit, in-VM replay-green
(`.verify-replay-evidence-linux-w42.txt` + `.admit-evidence-linux-w42.txt`). **W4.3
(macOS, NEW):** wrong-typed-timestamp row (A1 F3 fallout) — see its row below. Every
W4 row mints at 8c59ec4 (stub-backed rows recorded under 1.8.0, re-stamped to 1.8.1 by
the W3.1 dormant-seam re-stamp below); the two-field provenance (RECORDED-FROM +
MATCH-PROOF) now AGREES on every re-recorded row. Mutation-real grew 65→70 (W4.3
whole-row-drop + W4.2 termios-linux cooked→raw ×2).

**0b DELTA-STRENGTH W3.1 (2026-06-05, orc-4 Option C):** stub bumped 1.8.0→1.8.1 (the
env-gated `STUB_RAW_STDIN` seam; dormancy replay-verified + 36 proof files re-stamped,
`.restamp-evidence-w31.txt`). NEW row `send-pty-chunked-idle` (≥4KB chunked-IDLE path);
`send-pty-paste-burst` strengthened (ordered texts + anchor + reply). Mutation-real
grew 70→83 (R3 STUB_NO_QUEUE control + C2 seam-load-bearing control + 11 field-flip /
truncated / wrong-reply / swapped-order mutants). All W3.1 rows mint at 8c59ec4 /
stub-1.8.1.

## Required surfaces

| ✔ | Surface | Scenario script | Comparator class | Intended fixture path | Notes |
|---|---------|-----------------|------------------|-----------------------|-------|
| [x] | `ls/info --json` | `scenarios/ls_info_json.sh` | byte-exact | `fixtures/ls-info-json/normalized/ls-empty.json` | JSON contract; empty + populated registry |
| [x] | `buildClaudeCmd` w/ `CLAUDE_FLAGS` from config | `scenarios/build_claude_cmd.sh` | byte-exact | `fixtures/build-claude-cmd/normalized/cmd.txt` | `command` wrapper + flag order load-bearing. **Note (ADR-0004 ADD-9a)**: at pin CLAUDE_FLAGS is a CONSTANT (utils.ts:226-227), NOT config — "from config" describes the Rust target (ADR 0006). No class change. |
| [x] (EXPECTATION-ONLY, Rust-target) | `buildClaudeCmd` w/ a **NON-DEFAULT** config flag | `scenarios/build_claude_cmd_config.sh` | byte-exact | — (expectation inline in the scenario; no TS-recorded golden BY DESIGN) | **W1 P8 (panel opus M5 + gpt).** The default-flags row above passes a BROKEN config-loader that hard-codes the same triple (CLAUDE_FLAGS is a constant at the pin — utils.ts:226-227). This row asserts a NON-DEFAULT flag set, supplied via the ADR-0006 config seam (`QD_CLAUDE_FLAGS` tier-1), ACTUALLY reaches the launch argv — i.e. the loader READS config, not echoes a constant. **EXPECTATION-ONLY / Rust-target (ADR-0011 §a):** no TS counterpart — pinned TS ignores the seam (constant), so this would FAIL against TS by construction; recording the TS side would launder the constant-echo bug into gold. NO TS recording, NOT in the green gate. **TICKED at A2 pass-(b) (orc-3 ruling relay-1780669929563-1): first driven vs the Rust engine by A2 pass-(b) — verify.sh PASS rc=0 at main 7c8482d, evidence `dryrun/a2-passb-evidence.md`.** See ADR-0004 note (W1 P8) + ADR-0006. |
| [x] | zmx-dir resolution (ZMX_DIR tier) | `scenarios/zmx_dir_resolution.sh` | semantic-resolution-outcome | `fixtures/zmx-dir-resolution/normalized/resolution.txt` | explicit ZMX_DIR wins outright. **ADD-9a reclass byte→semantic (ADR-0004 DIV-9a-1)**: no print-resolved-dir surface; assert the OUTCOME (socket lands in the explicit dir). STUB-BACKED. |
| [x] | zmx-dir resolution (TMPDIR fallback + collapse) | `scenarios/zmx_dir_resolution_linux.sh` | semantic-resolution-outcome | `fixtures/zmx-dir-resolution/normalized/resolution-linux.txt` | Claude-spawns-Claude TMPDIR compounding → ONE canonical dir. **ADD-9a reclass byte→semantic (ADR-0004 DIV-9a-2)**: assert the COLLAPSE OUTCOME, not a fabricated byte line. **RECORDED on Linux (Lima `linuxvm` aarch64, Step 3)**: compounded `tmp/dup/dup` collapses to `tmp/dup`, socket lands `<TMPDIR>/dup/zmx-501`. STUB-BACKED. Double-recorded + admitted (copy-back via fixture_admit on devbox) + replay green IN-VM (`.verify-replay-evidence-linux.txt`; Linux-only — devbox has no /run/user). |
| [x] | **Bug-D dir matrix: `XDG_RUNTIME_DIR` tier** | `scenarios/zmx_dir_resolution_linux.sh` | semantic-resolution-outcome | `fixtures/zmx-dir-resolution/normalized/resolution-linux.txt` | **Linux-only (Lima `linuxvm`, aarch64). NEVER on macOS** — see exclusions. Inherits the ADD-9a resolution-outcome reframe. **RECORDED Step 3** on the REAL /run/user/501 tmpfs (ZMX_DIR unset → socket lands `<XDG_RUNTIME>/zmx`). STUB-BACKED. Double-recorded + admitted (copy-back via fixture_admit on devbox) + replay green IN-VM (`.verify-replay-evidence-linux.txt`). |
| EXCLUDED-no-stale-zmx-at-pin | preflight (`zmx send` capability) | — | — | — | **EXCLUDED (R4)**: at the pin against the real zmx 0.6.0, `assertZmxCapable()` is a NO-OP on the happy path (utils.ts:773-778 only errors when capability=="no"); the actionable-error path requires a TOO-OLD zmx lacking `send`, which cannot be produced on devbox (real zmx is 0.6.0). Honestly unrecordable even against the stub (the stub is `claude`, not `zmx`). The L3 lesson's invariant stays carried by LESSONS L3 + ADR; the negative path is a downgraded-zmx test owned by a later live instrument, not this oracle. |
| [x] | `new` byte trace + **boot-readiness EVENT** | `scenarios/new_session_trace.sh` | boot-readiness-event | `fixtures/new-session-trace/normalized/boot.trace` | PID-file appearance + went-busy; NOT blind-Enter (sanctioned divergence). **DELTA-STRENGTH W3.3 (P2) — boot strengthening, each field holding identically under TS blind-Enter AND the Rust ADR-0005 answerer (divergence guard):** `went_busy_observed=1`+`returned_idle_after_busy=1` (post-boot probe submit drives a real turn, status polled idle→busy→idle; STUB_BUSY_HOLD_MS=2000 widens the observation window), `input_chars_before_pidfile=0` (W2.3 STUB_COUNT_PRE_PID_STDIN counter, set INLINE; documentary `recording_mode=` in RECORDED-FROM), `decoy_rejected_matched_our_name=1` (a valid-shape DECOY pidfile for a DIFFERENT name pre-seeded; engine must match OUR name). Re-recorded at pin 8c59ec4 / stub 1.8.0; double-recorded + admitted + replay-green. Mutants (decoy-wins, no-went-busy, pre-pid-stdin-spam) all bite. |
| [x] | `send:pty` byte trace incl. **paste-burst** | `scenarios/send_pty_paste_burst.sh` | semantic-submit-discipline | `fixtures/send-pty-paste-burst/normalized/trace` | **RESOLVED at pin (was PROVISIONAL ADD-2)**: STUB-BACKED queue-to-busy (send-while-busy → queued → drains on idle) + JSONL-keyed `--wait`; markers key on application output, ASCII-only (B3 multibyte finding). recorded + replays green. **VERIFY-FIRST @ delta pin 8c59ec4: CONFIRMED no movement** (re-run byte-identical, same sha, user_record_count=2 unchanged — the burst payload is sub-1024B so `chunkText` emits a single chunk and the chunked-PTY commit 8c59ec4 is inert here). **DELTA-STRENGTH W3.1 (P3) — RESOLVED + STRENGTHENED (orc-4 Option C ruling 14:50 EDT).** Re-recorded at pin 8c59ec4 / stub 1.8.1: ADDS ordered user-record TEXTS (`user_text[0]=first-turn-holds-busy`, `user_text[1]=PASTE-BURST...` — red-team m4, not a queue_order_ok boolean), `anchored_on_user_text=` (the --wait anchor's text), and `wait_reply_text=` byte-exact (the deterministic STUB-REPLY). `assert_submit_discipline` keys on all of them. **NAMED CORPUS LIMITATION (no overclaim):** the busy row KEEPS a SUB-1KB (422B) payload — the cooked-mode busy path cannot host a ≥4KB single-canonical-line burst (macOS MAX_CANON=1024); the ≥4KB chunked delivery is covered by the NEW `send-pty-chunked-idle` row (raw mode) below, and busy-path ≥4KB REALISM by the A4 fake-REPL `DROP_OVER_BYTES` carrier (ADR-0009). Double-recorded; replay-green. Mutants: R3 STUB_NO_QUEUE control (busy-window burst discarded → outcome differs → submit-discipline RED), swapped-order, wrong-reply-text — all bite. See ADR-0011 W3.1-RULED addendum. |
| [x] | `send:pty` **CHUNKED-IDLE ≥4KB** delivery | `scenarios/send_pty_chunked_idle.sh` | semantic-chunked-idle | `fixtures/send-pty-chunked-idle/normalized/trace` | **DELTA-STRENGTH W3.1 (P3) NEW — closes the chunked-PTY corpus hole (orc-4 Option C).** STUB-BACKED, recording mode `STUB_RAW_STDIN=1` INLINE (documentary in RECORDED-FROM). Boots an IDLE stub session and sends ONE 4182B ASCII message (5 chunks at ≤1024B / ~150ms) via `send:pty --wait`; asserts at APPLICATION-OUTPUT level (R1, ADD-6): the FULL payload landed byte-exact in the JSONL user record (zero loss across all 5 chunks = the chunked-delivery contract), exactly 1 user record, anchored on the burst, `wait_reply_text` byte-exact, rc 0. R1 parity citations in the scenario header (send.ts:219-223 → deliverIdleTwoWrite submit.ts:287-334 → sendTextChunked :222-234 chunkBytes 1024/interChunkMs 150 → chunkText :196-213). The `STUB_RAW_STDIN` seam defeats the cooked-mode MAX_CANON=1024 cap (real Claude runs raw; ADR-0011 W3.1-RULED addendum). Re-recorded at pin 8c59ec4 / stub 1.8.1; double-recorded + admitted (L11-clean) + replay-green. Mutants (truncated-burst, full-payload-absent, wrong-reply, split-turn) + C2 seam-load-bearing control (seam-unset cooked-drop) all bite. |
| [x] | attach / detach / reattach | `scenarios/attach_detach_reattach.sh` | semantic (backlog-completeness + scroll-intact + no-altscreen + content-integrity) | `fixtures/attach-detach-reattach/normalized/reattach.trace` | zmx server-VT retention; dtach fails this. **B3 finding (attach re-resize)**: observed via `zmx history` (not a live attach), so no resize event in the capture — semantic class is resize-tolerant. **DELTA-STRENGTH W3.8 (P12) — content integrity:** SENTINEL byte-exact + EXACTLY-ONCE MULTISET (`assert_backlog_multiset_exact`) over the 20 whole lines — catches duplicates + banner-prefix corruption. Re-recorded at pin 8c59ec4 (re-mints MATCH-PROOF 0d0fa9e→8c59ec4, advances W4.1) / stub 1.8.0; replay-green incl. no-altscreen. |
| [x] | `history` | `scenarios/history.sh` | semantic (backlog-completeness + content-integrity) | `fixtures/history/normalized/history.trace` | serialized VT scrollback. **DELTA-STRENGTH W3.8 (P12) — content integrity beyond ordering:** a SENTINEL backlog line byte-exact (whole line) + an EXACTLY-ONCE MULTISET over the expected whole lines (`assert_backlog_multiset_exact`, on a WHOLE-LINE CR-stripped extraction) — catches DUPLICATES and BANNER-PREFIX corruption the -o ordering view cannot see. Re-recorded at pin 8c59ec4 (re-mints MATCH-PROOF 0d0fa9e→8c59ec4, advances W4.1) / stub 1.8.0; replay-green. Mutants (duplicated-sentinel, banner-prefixed) bite. |
| [x] | relay-health (**contract surface per ADD-5**: sidecar shape + `/message` + `/health` + `ls` join) | `scenarios/relay_health.sh` | byte-exact (contract shape) | `fixtures/relay-health/normalized/contract.txt` | **RESOLVED at pin (was PROVISIONAL)**: STUB-BACKED — records the engine CONTRACT only (sidecar + /health + POST /message + ls-join) against a LIVE in-jail relay endpoint; survives a transport-driver swap (ADD-5). **DELTA-STRENGTH W3.2 (P1) — value-bearing cross-checks on RAW PRE-normalization values (red-team B2), emitted as derived booleans/token the normalizer leaves intact:** `relay_sidecar_health_pid_same=1` (sidecar.pid==/health.pid), `sessionid_chain_eq=1` (sidecar==/health==ls-join sessionId), `message_id=mid-4fab57b3a2a5` (deterministic stub token for the fixed probe, byte-exact), `relay_pid_is_child_of_claude=1` (ps parentage) + `relay_claude_pid_distinct=1`. Field names avoid a trailing `pid=<n>` (the normalize_pids rule would eat it). Re-recorded at pin 8c59ec4 / stub 1.8.0; double-recorded + admitted + replay-green. Mutants (foreign-parent, stale-sidecar, empty-message-id, cross-field-sessionId) all bite in run_mutation_real. |
| [x] | exit codes 0 / 1 / 2 | `scenarios/exit_codes.sh` (1: `info` missing-session; 2: `config` unknown-sub) + `ls_info_json.sh` `.exit`=0 | exit-code | `fixtures/exit-codes/raw/*.exit` (+ `ls-info-json` `.exit`=0) | load-bearing; never normalized. Real exit-1/2 paths driven IN-JAIL (red-team M3 — was vacuous). Dry-run vs the **pinned prep clone** confirms 1 & 2 at the pin (`dryrun/captures/exit_codes/`, DRYRUN-NOT-ORACLE). |
| [x] | altscreen (passthrough emits zero) | `scenarios/attach_detach_reattach.sh` + layer-2 `run_layer2.sh` | no-altscreen | `fixtures/attach-detach-reattach/normalized/reattach.trace` | R1; asserted on every passthrough capture |
| [x] | boot-readiness event | `scenarios/new_session_trace.sh` | boot-readiness-event | `fixtures/new-session-trace/normalized/boot.trace` | EVENT contract (dup of new-trace row, called out per brief §8). **DELTA-STRENGTH W3.3 (P2)**: now also carries the went-busy transition (`went_busy_observed=1`), the pre-PID stdin count (`input_chars_before_pidfile=0`), and decoy rejection — see the new-trace row above. |
| [x] | **NEG: boot-timeout failure shape** (W3.4a, P4) | `scenarios/neg_boot_timeout.sh` | failure-shape | `fixtures/neg-boot-timeout/normalized/shape.txt` | **DELTA-STRENGTH W3.4a (P4) NEW.** STUB-BACKED, seam-active (STUB_WITHHOLD_PID=1 INLINE, recording_mode stamped). Records qd's FAILURE SHAPE when the counterpart withholds the PID file: rc=1 + `did not reach idle state within timeout` stderr token + no PID file (the panel's point — R2 seams previously had no RECORDED failure-shape fixture). Double-recorded at pin 8c59ec4 / stub 1.8.0; replay-green. Mutants (swallow-failure-as-success, stderr-token-stripped) bite. |
| [x] | **NEG: send:pty --wait no-reply shape** (W3.4b, P4) | `scenarios/neg_wait_no_reply.sh` | failure-shape | `fixtures/neg-wait-no-reply/normalized/shape.txt` | **DELTA-STRENGTH W3.4b (P4) NEW.** STUB-BACKED, seam-active (STUB_WITHHOLD_JSONL=1 INLINE). R4-HONEST SHAPE: `--wait` finds the user anchor but the withheld reply + reached-idle means the wait COMPLETES (not a timeout) and prints `(no text response)`, rc=0; JSONL has 1 user / 0 assistant records. (A true `--wait` timeout would need a never-idle counterpart — a different seam, out of scope.) Double-recorded; replay-green. Mutants (fabricated-reply, assistant-record-appeared) bite. |
| [x] | **NEG: relay-unhealthy outcome shape** (W3.4c, P4) | `scenarios/neg_relay_unhealthy.sh` | failure-shape | `fixtures/neg-relay-unhealthy/normalized/shape.txt` | **DELTA-STRENGTH W3.4c (P4) NEW.** STUB-BACKED, seam-active (STUB_DEAD_HEALTH=1 INLINE). SPEC-PREMISE DIVERGENCE (flagged): at the pin NO qd surface consumes /health for the relay (ls-join reads the SIDECAR; scanRelayPorts is the no-sidecar fallback scanning 8900-8999 only). The row records the REAL engine property — ls-join is SIDECAR-DRIVEN + HEALTH-INDEPENDENT (stays joined through a dead /health). Empirically probed. Double-recorded; replay-green. Mutant (ls-join re-gated on /health) bites. |
| [x] | **NEG/TOL: two-stage partial PID write** (W3.4d, P11) | `scenarios/neg_two_stage_tolerance.sh` | failure-shape | `fixtures/neg-two-stage-tolerance/normalized/shape.txt` | **DELTA-STRENGTH W3.4d (P4/P11) NEW.** STUB-BACKED, seam-active (STUB_TWO_STAGE_PID_WRITE=1 INLINE, ~1500ms gap). OUTCOME-ONLY per red-team m3: boot reaches idle through the partial writes, ls never crashes DURING the write window, session visible AFTER — NEVER the racy partial state. SCN_BUDGET_MS absorbs the gap. Double-recorded; replay-green. Mutant = partial-JSON-INTOLERANT reader (pre-A1-PR#20 whole-row-drop → session_visible_after 1→0) + ls-crash; both bite. |
| [x] | **NOISE: stale registry/sidecar shapes** (W3.4e, P4) | `scenarios/noise_stale.sh` | failure-shape | `fixtures/noise-stale/normalized/shape.txt` | **DELTA-STRENGTH W3.4e (P4) NEW.** Pre-seeded (no seam, no boot). One row, a small family: duplicate same-name SAME-sessionId PID files dedup to ONE row; dead-pid registry entry STILL visible (no liveness filter at pin); stale relay sidecar (dead pid) does not crash `qd ls`. The wrong-TYPED-timestamp registry row is **W4.3's** (LANDED — its own row below), not here. Double-recorded; replay-green. Mutants (dup-not-deduped, dead-pid-dropped) bite. |
| [x] | **live-attach FIRST WINDOW** (W3.5, P5) | `scenarios/attach_first_window.sh` | semantic-presence | `fixtures/attach-first-window/normalized/presence.txt` | **DELTA-STRENGTH W3.5 (P5) NEW.** STUB-BACKED. The attach-INITIATING capture (B3 ruled only attach-SPANNING out). Boots, drives a deterministic backlog (EMIT 7), initiates a REAL `qd attach` under a PTY, captures the first ~1KB, detaches CLEANLY (ctrl-\). SEMANTIC/PRESENCE class (red-team M1, binding — NOT byte-exact over the reflow-variable KB): `?1049h` ABSENT + the `SBLINE 7` backlog sentinel PRESENT. ENV hazard handled: attach child runs `env -u ZMX_SESSION` (else nested-zmx attach redirects to the host session). Double-record STABLE across 3 runs (M1 stability check passed — NOT EXCLUDED). Mutants (altscreen-in-window, sentinel-absent) + a no-altscreen mechanism proof bite. |
| [x] | **hostile-cwd project-path mapping** (W3.6, P9) | `scenarios/hostile_cwd.sh` | semantic-project-path | `fixtures/hostile-cwd/normalized/mapping.txt` | **DELTA-STRENGTH W3.6 (P9) NEW.** STUB-BACKED. Boots from a jail cwd `p9.host:8080_v1.2`; asserts cwdToProjectPath maps `/`→`-` ONLY with `:`/`.`/`_` PRESERVED VERBATIM (session.ts:433-435), the JSONL lands at the mapped dir, and send:pty --wait works from the hostile cwd. Derived booleans PRE-normalization (B2 idiom); robust to the macOS /tmp→/private/tmp realpath indirection. Double-recorded; replay-green. Mutants (wrong-mapping/specials-sanitized, jsonl-at-wrong-dir) bite. |
| [x] | **termios raw-mode report** (W3.7/W4.2, P10, macOS + Linux) | `scenarios/termios_report.sh` (macOS) + `scenarios/termios_report_linux.sh` (Linux) | semantic-termios | `fixtures/termios-report/normalized/report.txt` (macOS) + `report-linux.txt` (Linux) | **DELTA-STRENGTH W3.7 (P10) NEW, PLATFORM-SPLIT** (RECORDED-FROM.macos/.linux + MATCH-PROOF.macos/.linux siblings; **Linux side RECORDED in W4.2 in-VM — Lima `linuxvm` aarch64, uid 501, pin 8c59ec4, double-recorded + copy-back admitted via platform-aware fixture_admit + replay-green in-VM** `.verify-replay-evidence-linux-w42.txt`). STUB-BACKED. Submits the `STTY` prompt (1.8.0 reporter), captures STTY-REPORT, asserts the SEMANTIC fields (icanon/echo/isig). FIDELITY BOUNDARY (flagged): the engine does NOT set raw mode — the TUI app does, and the stub is a LINE READER, so the session PTY is the default zmx-run COOKED mode (icanon=1 echo=1 isig=1, SAME on macOS AND Linux — the platform-INDEPENDENT cooked contract; only the forensic flag BITMASKS differ, the platform-split reason). The cheap engine-behaviour portion per ADR-0011 §(a); raw-TUI realism stays A4/C2. Mutant = the INVERSE of the spec's cooked-mode example (cooked→raw icanon/echo 1→0) because recorded reality is cooked — bites on BOTH sides. Double-recorded; replay-green (resolves .macos on Darwin, .linux in-VM). |
| [x] | **NOISE: wrong-typed-timestamp registry row** (W4.3, A1 F3) | `scenarios/wrong_typed_timestamp.sh` | failure-shape | `fixtures/wrong-typed-timestamp/normalized/shape.txt` | **DELTA-STRENGTH W4.3 (orc-3 succession item) NEW.** Pre-seeded (no seam, no boot), joins the W3.4e stale-noise family, recorded as its OWN row. A registry file with ISO-STRING `startedAt`/`updatedAt` (the shape stub 1.6.0 wrongly wrote — numbers are the declared `PidEntry` type, session.ts:68-69), every OTHER field valid. Records the TS engine's ls OUTCOME at pin 8c59ec4: the dynamic read TOLERATES (bare `JSON.parse` + `as PidEntry` cast, no runtime validation; `new Date(isoString)` round-trips) → **row VISIBLE, `qd ls` exit 0** (empirically confirmed). Matches the A1 expectation: NO whole-row drop. Rust matches this PRESENCE through the A1 PR#20 per-field-permissive deserializer (`RegistryEntry::from_value`); byte-parity for the wrong-typed field NOT claimed — PRESENCE is the contract. Derived booleans PRE-normalization (`ls_exit_zero`, `wrong_typed_row_visible`). Double-recorded; replay-green. Mutant (whole-row-drop, modelling the pre-A1-PR#20 Rust behavior: `wrong_typed_row_visible` 1→0) bites. |

## Synthetic layer-2 (Part 1 — no pin; these ARE wired & green)

| ✔ | Surface | Harness | Comparator class | Status |
|---|---------|---------|------------------|--------|
| [x] | 64KB ANSI burst ×100 | `fixtures/layer2/run_layer2.sh` | no-altscreen + no-drop + budget | GREEN (synthetic, Part 1) |
| [x] | SIGWINCH storm during streaming | `fixtures/layer2/run_layer2.sh` | no-altscreen + content-survived + budget | GREEN (synthetic, Part 1) |
| [x] | dirty-state JSON corpus | `crates/golden` (`cargo test -p golden`) | permissive-parse (legacy/missing/corrupt/partial) | GREEN (synthetic, Part 1) |
| [x] | W4: wrong-TYPED registry field (string `pid`/`startedAt`) | engine: `crates/qd` `registry::parse_file` (`wrong-typed.json`); reference: `crates/golden` whole-row clean-fail | per-field-permissive (engine: row survives, field degraded, `degraded` list + `QD_DEBUG` line) vs whole-row serde (reference: clean Err) | GREEN (A4 pass-(b) F3 RESOLVED by A1 pass-(b)) |

(Layer-2 rows are synthetic and fully asserted since Part 1 — they need no TS pin.
The required-surface rows above are all ticked as of the DELTA-STRENGTH close.)

## Recorded exclusions (NOT silent skips)

These surfaces are deliberately OUT of the corpus and will not get rows:

| Excluded surface | Reason |
|------------------|--------|
| `spawn` (Claude-spawns-Claude orchestration) | Parked under qb (spec §4 OUT; brief: "EXCLUDES spawn surfaces"). |
| `qb` parked verbs | Parked; SPAWN-* battery rows are recorded exclusions per ADD-1, not skips. |
| substrate / plugins surfaces | Out of the engine corpus (spec §4 OUT). |
| `XDG_RUNTIME_DIR` tier **on macOS** | The Bug-D `XDG_RUNTIME_DIR` tier exists only on Linux; on macOS there is no such tier — the row above is Linux-only by construction, and the macOS variant is an explicit exclusion, not a missing test. |

## Pin gate (Part 2 entry condition) — SATISFIED; Part 2 COMPLETE

Part 2 (recording, RECORDED-FROM stamps, tick-off, mutation vs real corpus) started
when the pinned TS commit `0d0fa9ed4800efb1309eca2311345c48af2c4932` (zmx 0.6.0) was
supplied to `record.sh` (`PINNED_TS_COMMIT`) and relay was confirmed-working.
PROVISIONAL rows re-verified at that pin (Step 2, all resolved).

**Part-2 completion state (Steps 0-4):**
- ALL required-surface rows are `[x]` ticked (double-recorded + admitted + replay
  green) OR `EXCLUDED-<reason>` (R4). ZERO unticked rows. Tick counts: 13 required
  `[x]` + 1 `EXCLUDED` (preflight) + 3 synthetic layer-2 `[x]`. (Post-Part-2: + 1
  EXPECTATION-ONLY `[x]` — W1 P8, ticked at A2 pass-(b) once the Rust engine first
  drove it; + the W4 layer-2 row added at A4/A1 pass-(b).)
- macOS rows (Step 2, Darwin arm64) + Linux rows (Step 3, Lima `linuxvm` aarch64:
  TMPDIR-collapse + XDG_RUNTIME_DIR tiers; copy-back admitted via fixture_admit on
  devbox; replay green in-VM — see `fixtures/.verify-replay-evidence{,-linux}.txt`).
- Mutation vs the REAL corpus + the intentionally-divergent TS invocation + the R2
  stub-seam negative controls all GREEN, zero false negatives — see
  `mutation/EVIDENCE-mutation-real.txt` (`run_mutation_real.sh`, 27/27 — incl. the
  R2 seams exercised under each row's ACTUAL recorded class alongside byte-exact)
  and the Part-1 synthetic teeth (`run_mutation.sh`, 10/10, intact).
- Sanctioned-divergence ADR: `doc/adr/0011-sanctioned-divergences.md` (R3, renumbered
  from 0008 for cross-branch dedup). Stub sha in every RECORDED-FROM it backs (R1).

## A4 named divergences + A4 rows (lead-maintained; full detail in ADR 0007-0009 + LESSONS L4/L12-L15)

| Surface | Class | Where ruled/recorded |
|---------|-------|----------------------|
| `qd new -p` exit contract 0/10/1 (TS: always 0) | sanctioned divergence (HARDENING #3) | ADR 0008; `scenarios/new_went_busy_exit.sh`; PROTOCOL.md |
| Idle-path delivery = two-write + content-verified CR + chunked text phase (TS @ delta pin: two-write `e7dab5d` + chunked `8c59ec4`) | **PARITY at delta pin 8c59ec4** — reclassified from mode (b) sanctioned divergence (TS single-write @ 0d0fa9e) once TS adopted two-write delivery (`e7dab5d`) and chunked text (`8c59ec4`); Rust and TS now match on both modes. Cites TS commits e7dab5d + 8c59ec4. | ADR 0009 (amended; delta note); fakerepl R4 rows + jumbo/negctl-pairing rows; upstream report consumed by parity flip |
| `send:relay` missing/malformed message_id → loud exit 1 | **PARITY at delta pin 8c59ec4** — reclassified from TS-bug class (TS printed "undefined" + exit 0 @ 0d0fa9e) once TS adopted loud error + exit 1 (`92910a4`), matching Rust. Cites TS commit 92910a4. | journal 2026-06-05; ADR 0009 delta note; upstream report consumed by parity flip |
| `send:relay --wait` retries ONLY connection-drop (TS: any non-abort error) | TS-contradicts-own-help; Rust conforms to documented contract | journal 2026-06-05 (not-worth-reporting note) |
| `strip_ansi` bare lone-ESC: ESC+any-byte stripped (TS regex keeps non-[@-_] byte) | in-function edge, boot-corpus-sanctioned; inert for composer | `sendpty.rs` w6_* pinned tests; M1 journal ruling |
| Name-reject at create (`/ \ .. NUL` refused; TS forwards) | Class-4 narrowing | ADR 0007 (amended); create.rs tests |
| `qd wait` completion = status + TRANSCRIPT-CONTENT keyed (done = idle AND (saw_busy OR end_turn-record past entry offset); degrades warn-once to status-only on transcript absent/shrunk). **Anti-"fix" note RETIRED-WITH-REASON** (wart-wave): the A4-era "stays status-keyed" row guarded against an UNSANCTIONED semantic fork; Pete sanctioned the fork 2026-06-05 19:47 EDT (ADD-15 W7, continuity WAIVED; W6 close-the-window). The note did its job — the fork waited for sanction. | sanctioned divergence (ADD-15 W6+W7); reliability-over-parity (ADD-13(2)) | `wait.rs` module doc + w6_*/negctl_*/mutation_*/probe_* rows; EVIDENCE-wart-wave-m3.txt (structural RED-pre-fix); exec/divergence-table.md W6+W7 row |
| `qd wait` residual completion window (NARROWED by W6 fix; was: any turn inside one 500ms poll): a turn completing BEFORE the loop's entry-time transcript offset while the resolved status is stale-non-idle — both sources miss it (status never non-idle in-loop, record pre-offset). Named, bounded, not closed. `deliver_prompt`'s 250ms acceptance-window row (L14, fakerepl soak floor) unchanged — that window is the SUT-poll constraint, distinct from this one. | inherited-limitation class, narrowed (TS retains the FULL W6 window) | wait.rs probe_offset_excludes_pre_entry_records (the boundary is pinned, not hidden); LESSONS L14 |
| `--wait` JSONL shrink/rotation → loud SourceError exit 1 (TS: silent whole-file fallback, possible wrong anchor) | Rust-only guard (ADD-8 W5, merge-ruling ruled) | send.rs read_lines guard + sendpty.rs unit row |
| Chunked-submit verify-after-submit (wart-wave W8, ADR-0012): post-went-busy transcript read-back; loud `payload truncated` exit-1 ONLY on positive truncation evidence; qd-new-p best-effort/degrading vs send:pty-idle strict; no-wait busy-queue sliver NAMED (D16 remainder); single-chunk submits byte-identical (trigger = production chunk_text count, 1024/1025 + multibyte seams pinned) | sanctioned divergence (ADD-15 W8 / M11); the wave's one exit-behavior delta (existing exit-1 class, no new codes — ADR-0008 note) | 12 verify-policy unit rows (10 verify_* + 2 needs_verify_* seam rows); 5 w8_* fakerepl gate rows (stall/saturation + convo seams; mutation + 2 negctl); new_went_busy_exit.sh W8 legs (QA-lane, A4_RUN_LIVE) + EVIDENCE-wart-wave-m5.txt (RED-without-wiring) |
| `--model` sends COMBINED `/model <m>\r` through the shared chunked helper (W1 exception MOOT BY CONSTRUCTION, NOT a divergence: ≤1024B ⇒ one chunk ⇒ byte-identical to TS `sendViaZmx` single combined write, `8c59ec4:lifecycle.ts:374-385`) | ADR 0009 amendment (ADD-8 W1 superseded; TS parity preserved) | ADR 0009 (amended) §W1-moot; lifecycle.rs call-site comment |
| `fetch_reply` rejects CR/LF/space in server-supplied message_id | Rust-only guard (ADD-8 N1) | relay_http.rs + unit row |
| Registry read: a wrong-TYPED field (e.g. string `startedAt` where TS declares number, session.ts:68) → ~~Rust whole-row serde drop, session silently invisible to ls/resolve~~ (TS dynamic read renders the row) | **RESOLVED by A1 pass-(b)** per-field-permissive deserializer (`RegistryEntry::from_value`, `crates/qd/src/registry.rs`): each field parses independently via `serde_json::from_value`; a wrong-typed field DEGRADES to default (`None`) and the ROW SURVIVES (visible to ls/resolve again). Degraded field-names carried on `ScannedEntry::degraded`/`TombstonedEntry::degraded` (future `qd doctor` surface) + one `QD_DEBUG=1`-gated stderr line per degraded/skipped file (default SILENT → stdout byte-parity unchanged). NAMED residual: Rust renders the row with the field DEFAULTED; TS renders the RAW value — byte-parity for wrong-typed rows NOT claimed, the row's PRESENCE is the contract (no string→number coercion). **Was:** L8 permissive-parse hole — `#[serde(default)]` covers MISSING, not WRONG-TYPED; Rust dropped the whole row, session silently invisible (TS rendered it). Production impact low (TS qd + claude write numbers; R5-R7 live sessions resolved fine). W4 corpus row added: `fixtures/layer2/dirty-state/wrong-typed.json`. | A4 journal 2026-06-05 pass-(b) (parity control: pinned-TS ls sees the row, Rust ls did not, same jail/session); orc-3 ruling relay-1780664987618-6. **Resolution:** `registry.rs` `from_value`/`parse_file` + tests `wrong_typed_*`, `mixed_dir_wrong_typed_and_clean_both_present`, `dirty_state_wrong_typed_fixture_survives`, `debug_warn_silent_by_default_and_gated_on`; reference-shape clean-failure asserted in `crates/golden/tests/dirty_state_corpus.rs::wrong_typed_fixture_is_clean_whole_row_failure_in_reference_shape` |

A4 recorded exclusions (carried, not silent): OpenCode paths in `qd wait`/`send:http`
(parked ruling); real-claude-on-Linux live boot (VM unauthenticated — NAMED, carries
to A7; fake-claude Linux smoke green); live `send:pty --wait`/`qd wait` attestation
(driver R3; CLOSED by boot #6/R5 except the qd-wait-timeout live row — attested by
deterministic oracle, lead-ruled no further boot). A7 hardening-sweep carries: relay
chunked decoder body-size cap + wall-clock budget (ADD-8 W2, merge-ruling named).
