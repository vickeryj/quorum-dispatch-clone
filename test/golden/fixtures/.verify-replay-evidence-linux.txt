# Verify-replay evidence (Linux tick proof) — IN-VM fresh-jail end-to-end green
# pin 0d0fa9e, zmx 0.6.0, Linux aarch64 (Lima sbtest), recorded+verified 2026-06-05
#
# WHY in-VM (not brano): the Linux zmx-dir-resolution rows exercise the
# XDG_RUNTIME_DIR tier (/run/user/<uid>, a Linux systemd construct ABSENT on macOS
# — brano has no /run/user) and the TMPDIR-collapse OUTCOME. These can only be
# AUTHENTICALLY replayed on Linux, so verify runs in-VM and this output is the
# captured tick evidence (per Step-3 brief: "if a row can only replay in-VM, run
# verify in-VM and capture the output as evidence").
#
# Recorder rooted the jail under /run/user/501 (TMPDIR=/run/user/501) so the XDG
# tier hits the REAL Linux runtime tmpfs while every resolved socket dir stays
# under JAIL_ROOT (sbrg-prefixed) — hermetic, zero leftovers.
#
zmx_dir_resolution_linux: [verify] PASS: zmx-dir-resolution-linux
  case=XDG_RUNTIME_DIR_tier  resolved==expected (<XDG_RUNTIME>/zmx)
  case=TMPDIR_collapse_tier  resolved==expected (<TMPDIR>/dup/zmx-501; collapse(dup/dup)=dup)
TICK SUMMARY: 2/2 Linux rows replay green in-VM (TMPDIR-collapse + XDG_RUNTIME_DIR tiers)
